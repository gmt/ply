# Codex Agent Instructions

Codex has full permission to modify any files in this repository and to run tests
or other commands needed to complete tasks. There are no special coding
restrictions beyond standard Python style. After making changes, run the
following commands:

```
python3 -m pip install -e .
python3 -m unittest discover -v
```

All pull requests must include a summary of changes and the test results.
